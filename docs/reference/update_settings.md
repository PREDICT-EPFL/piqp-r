# Update settings of a PIQP model

Update settings of a PIQP model

## Usage

``` r
update_settings(model, ...)
```

## Arguments

- model:

  A `piqp_model` object

- ...:

  not used

## Value

invisible NULL
