# Get settings of a PIQP model

Get settings of a PIQP model

## Usage

``` r
get_settings(model, ...)
```

## Arguments

- model:

  A `piqp_model` object

- ...:

  not used

## Value

a list of current settings
