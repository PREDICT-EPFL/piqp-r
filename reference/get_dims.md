# Get dimensions of a PIQP model

Get dimensions of a PIQP model

## Usage

``` r
get_dims(model, ...)
```

## Arguments

- model:

  A `piqp_model` object

- ...:

  not used

## Value

a list with named elements `n`, `p`, and `m`
